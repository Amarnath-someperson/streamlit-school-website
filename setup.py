import setuptools

setuptools.setup(
    install_requires=[
        "PyJWT >=2.3.0",
        "bcrypt >= 3.1.7",
        "PyYAML >= 5.3.1",
        "streamlit >= 1.18.0",
        "streamlit-authenticator >=0.2.3" 
        "extra-streamlit-components >= 0.1.60"
    ],
)